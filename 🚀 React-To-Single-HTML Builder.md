# 🚀 React-To-Single-HTML Builder

**React-To-Single-HTML** — это автономный инструмент (Single-File Application), который позволяет мгновенно превратить исходный код React-проекта в **один портативный HTML-файл**.

Этот инструмент идеально подходит для разработчиков и пользователей нейросетей (Google AI Studio, ChatGPT, Claude), которым нужно быстро запустить сгенерированный код локально без ручной настройки конфигурации сборки.

![License](https://img.shields.io/badge/license-MIT-blue.svg)
![Platform](https://img.shields.io/badge/platform-macOS%20%7C%20Linux-lightgrey)
![Stack](https://img.shields.io/badge/stack-React%20%7C%20Vite%20%7C%20SingleFile-blueviolet)

## ✨ Для чего это нужно?

Обычно, чтобы запустить React-приложение и собрать его в один файл, нужно:
1.  Создать проект через Vite.
2.  Установить плагин `vite-plugin-singlefile`.
3.  Правильно настроить `vite.config.ts`.
4.  Поправить `package.json`.

**React-To-Single-HTML** делает это за вас. Вы просто перетаскиваете папку с кодом (`src`) в окно браузера, и инструмент генерирует готовый **Bash-скрипт**, который сам установит зависимости, настроит сборку и выдаст вам готовый `index.html`.

## 🛠 Возможности

*   **Zero-Install:** Сам инструмент — это один файл `builder.html`. Не нужно ничего устанавливать, просто откройте в браузере.
*   **Drag & Drop:** Поддержка перетаскивания папок и файлов.
*   **Авто-конфигурация:** Автоматически создает системные файлы (`vite.config.ts`, `tsconfig.json`, `package.json`), настроенные на Single-File сборку.
*   **Гибкость:** Выбор места создания проекта (Рабочий стол, Загрузки, Домашняя папка).
*   **Редактор кода:** Встроенный просмотр и редактирование файлов перед генерацией.
*   **Экспорт:** Генерация команд для терминала или скачивание готового `.sh` скрипта.

---

## 📋 Требования

Для работы **сгенерированного скрипта** на вашем компьютере должны быть установлены:
*   **Node.js** (версии 18 или выше)
*   **npm** (обычно идет в комплекте с Node.js)
*   Терминал (macOS / Linux / WSL на Windows)

---

## 🚀 Инструкция по использованию

### Шаг 1. Запуск Builder'а
Скачайте файл `builder.html` из этого репозитория и откройте его в любом современном браузере (Chrome, Safari, Edge).

### Шаг 2. Подготовка файлов (Кейс AI Studio)
Если вы скачали код из нейросети (например, Google AI Studio), файлы часто лежат "в куче" в одной папке. React требует, чтобы исходный код лежал в папке `src`.

1.  В инструменте справа, в разделе **"Инструкция"**, скопируйте команду подготовки.
2.  Откройте терминал в папке с вашими скачанными файлами.
3.  Выполните команду:
    ```bash
    mkdir -p src && mv App.tsx index.tsx logic.ts types.ts constants.ts index.css src/ 2>/dev/null || true
    ```
    *Это переместит файлы кода в папку `src`.*

### Шаг 3. Импорт в Builder
1.  Перетащите папку `src` в центральную область окна `builder.html`.
2.  Вы увидите отчет об успешном импорте.
3.  В левой панели появятся ваши файлы (синие) и системные файлы конфигурации (серые).

### Шаг 4. Настройка и Генерация
1.  В левой панели укажите **Имя проекта** (например, `my-calculator`).
2.  Выберите **Где создать** папку проекта (например, `Рабочий стол`).
3.  Нажмите большую синюю кнопку **"Сгенерировать Bash"**.

### Шаг 5. Запуск
У вас есть два пути:

**Вариант А: Копирование (Быстро)**
1.  Нажмите кнопку копирования в появившемся окне.
2.  Вставьте код в свой Терминал и нажмите `Enter`.

**Вариант Б: Скачивание файла (Надежно)**
1.  Нажмите кнопку скачивания (📥), чтобы сохранить файл `install.sh`.
2.  В терминале перейдите в папку с файлом и дайте права на выполнение:
    ```bash
    cd ~/Downloads
    chmod +x install.sh
    ./install.sh
    ```

---

## 📂 Структура проекта (Что внутри?)

Инструмент автоматически создает следующую структуру для вашего приложения:

```text
my-project/
├── package.json          # Настроен с vite-plugin-singlefile
├── vite.config.ts        # Конфигурация для встраивания ресурсов в HTML
├── tsconfig.json         # Настройки TypeScript
├── index.html            # Точка входа
└── src/                  # ВАШ КОД (который вы перетащили)
    ├── App.tsx
    ├── index.tsx
    └── ...